# Capella Integrated Global Limited website

## Run locally

1. Install dependencies with `npm install`.
2. Start the development server with `npm run dev`.
3. Create a production build with `npm run build`.

Contact details and complete service information are pending client confirmation. The current site intentionally does not process or store enquiries.
